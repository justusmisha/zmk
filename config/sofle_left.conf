# Экран (с модулем nice_oled)
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=n

# Оставляем собачку, убираем WPM и слой
CONFIG_NICE_OLED_GEM_ANIMATION=y
CONFIG_NICE_OLED_WIDGET_WPM=n
CONFIG_ZMK_WIDGET_LAYER_STATUS=n

# 🔧 ВАЖНО: отключить встроенный виджет батареи ZMK,
# чтобы не дублировался с виджетом батареи из nice_oled
CONFIG_ZMK_WIDGET_BATTERY_STATUS=n

# Две батареи по BLE (флаги остаются, они нужны центральной стороне)
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y

# (опционально)
# CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y  # <-- это для встроенного виджета ZMK; для nice_oled не нужно
